using UnityEngine;

public class EventMateChoice : MonoBehaviour
{
   /* public float time;
    public float discontTime;
    bool BuyTime = true;
    [SerializeField] Image Space;
    [SerializeField] Image Clock;
    public void Control(float time)
    {
        if(BuyTime)
        {
            this.time = time;
            But = false;
        }
        discontTime += Time.deltaTime;
        time -= discontTime;
        if(time => 0f)
        {
            Space.SetActive(true);
            Clock.fillAmount = time.normalized;
        }else
        {
            Space.SetActive(false);
        }
    }*/
}
